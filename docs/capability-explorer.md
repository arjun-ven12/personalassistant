# Capability Explorer

Capability Explorer displays every reviewed provider and its finite semantic
capabilities.

For each capability it shows:

- inputs;
- outputs;
- permissions;
- dependencies;
- risk;
- verification;
- examples.

The explorer is intentionally transparent: unsupported operations are not
hidden behind a generic execute field. If a capability is not declared, it
cannot be dispatched.
