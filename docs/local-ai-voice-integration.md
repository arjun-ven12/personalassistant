# Local AI and Voice

Voice remains deterministic-first. A future voice integration can call the
interpretation role only after Human Understanding reports the AI-router band,
then return the validated result to the existing Intent Engine. Voice or a local
model never approves high-risk work and an unavailable runtime must resolve to a
controlled error or clarification rather than leaving `UNDERSTANDING` active.
