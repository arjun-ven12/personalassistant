# Local Model Registry

`gemma3-4b` is registered for natural-language interpretation, local reasoning,
conversation, and structured extraction. The registry records runtime, model
name, roles, memory class, and capabilities. `toolCalling` is permanently false
for Phase 20A. Future models are added by registration and role mapping.
